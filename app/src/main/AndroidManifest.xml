<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nf.iteris.com.br.iterisapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ico_cell"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ico_cell"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home_screen.HomeScreenActivity" />
        <activity android:name=".ui.sign_up.SignUpActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.list_nf.ListNfActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".ui.nf_registration.NfRegistrationActivity" />
        <activity android:name=".ui.antecipation_request.AntecipationRequestActivity"></activity>
    </application>

</manifest>